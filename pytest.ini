[pytest]
testpaths = tests_spend
norecursedirs = validation data logs run backups work bin .git .venv .venv-312 .venv-spend
addopts = -q
markers =
    browser: requires local Chrome; uses synthetic fixtures and controlled network responses
